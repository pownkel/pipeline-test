trigger:
- none

resources:
  pipelines:
  - pipeline: trigger-pipeline
    source: Trigger pipeline
    trigger:
      branches: [master]

steps:
- script: echo Pipeline A
  displayName: 'Run a one-line script'
